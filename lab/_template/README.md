# Topic name

One line: what this topic is, in words a newcomer already has.

## The question

One sentence: the single question this topic answers.

## What we connect

Which of Waku's agent, memory and skills this topic uses, and what it connects
them to.

Verified against: tool version, YYYY-MM-DD

## Run it

One command, run from the repo root, plus any `pip install` it needs.

## What we found

Facts and numbers, each one checked. Say what was measured, and on what.

## Video angle

A brief, not a script: the hook, the one surprising finding, and which board
to film (a PNG screenshot in `screenshots/`; the board's source stays private).

## Graduation

What, if anything, should move into `waku/`, and at which rung of the
footprint ladder. Once it moves, say where it went.
