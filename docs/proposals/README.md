# Design proposals

Throwaway HTML mockups, so a design can be looked at and decided before any
product code changes.

The flow is: propose, decide, implement, delete. Once a design is built into
the real product, its mockup here is deleted.

`*.html` in this folder is git-ignored. Only this README is tracked.
